# version = "0.1.6"
text:
  # RGB colour for text
  red: 255
  green: 200
  blue: 55

back:
  # RGB colour for shadow or outline
  red: 0
  green: 0
  blue: 0
  
display:
  # Whether to leave last payed on display when stopped, or
  # display splash screen
  # 0 = leave last played
  # 1 = display splash
  splash: 0
  
